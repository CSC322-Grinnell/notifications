%h1 Listing of Classrooms

%table{:style=>"border: 1px; width: 50%"}
  %tr
  
  - current_user.classrooms.each do |room|
    %tr
      %td= link_to room.name, room
      - if admin?
        %td= link_to 'Remove', room, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

- if admin?
  = link_to 'Add New Classroom', new_classroom_path
