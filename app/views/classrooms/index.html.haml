%h1 Classrooms

%table{:style=>"border: 1px; width: 50%"}
  %tr
  
  - current_user.classrooms.each do |room|
    %tr
      %td.links= link_to room.name, room
      - if admin?
        %td.del= link_to 'Remove', room, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

.new
  - if admin?
    = link_to 'Add New Classroom', new_classroom_path
